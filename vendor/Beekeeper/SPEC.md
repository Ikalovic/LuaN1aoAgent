本项目（Beekeeper）将作为Web智能渗透Agent的一个模块来使用，将实现对于社工库（目前只考虑账密社工库）的存放与提取
具体实现为
1. 实现两个fastapi接口，一侧是输入，一侧是输出，输入为<域名,账户,密码>的三元组，输出时使用域名查询，返回<账户,密码>的二元组
2. 实现MCP工具，同样实现输入输出，不过这个是让AI自行测试用的，应只返回少量信息，正式枚举的时候要求AI结合目标站点信息编写脚本进行枚举，而不能直接在MCP进行枚举
3. 实现一个WebUI，用于浏览社工库的数据，同时可以进行数据管理