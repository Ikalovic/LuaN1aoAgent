# 青玄前端首版交付

日期：2026-09-15。分支：`feat/qingxuan-frontend`。首页设计与计划已提交为 `537c3a9`；实现仍保留在当前工作区，未合并或推送。

## 本地入口

- 首页：http://127.0.0.1:5173/
- 独立登录：http://127.0.0.1:5173/?page=login
- 工作台：http://127.0.0.1:5173/?view=overview
- 独立态势大屏：http://127.0.0.1:5173/?view=wallboard
- 构建版本及 API：http://127.0.0.1:8787/

沿用现有登录认证。先选择已有运行，再进入大屏，可保留该运行上下文。没有运行数据时不会生成演示资产或模拟攻击。

## B 风格首页与独立登录

- 已按确认的 B 方案实现：首页采用整幅石墨背景、青玄标题、简短介绍与能力条，下方是浅色产品概览，可切换工作台和态势大屏截图。
- 首页无需登录，不读取业务快照，不挂载工作台或三维场景。产品截图明确注明使用本地演示数据，不包含实际运行信息。
- `?page=login` 保留真正的登录、注册、表单校验、错误反馈、深浅主题和中英文切换。登录后返回原来的工作台或大屏位置，包括运行及图谱参数；不接受外部回跳地址。
- 登录页和工作台品牌入口可返回首页。工作台沿用未保存重放草稿的离开确认；返回首页不会退出登录或停止后端任务，并会清理前端轮询。
- 首页和登录页均包含小字署名及链接：本项目基于 LuaN1aoAgent 改造而成。

设计见 [首页与登录设计](superpowers/specs/2026-09-15-qingxuan-homepage-design.md)，完成记录见 [实施计划](superpowers/plans/2026-09-15-qingxuan-homepage-implementation.md)。本轮没有生成新的 AI 图片，复用现有品牌和背景，并导出两张产品截图。

本轮验证：7 个文件、30 项定向前端测试通过；`npm run build:web` 和 `git diff --check` 通过。`scripts/check-qingxuan-entry.mjs` 使用浏览器专用认证和 API 夹具，在 1920×1080、390×844、3840×2160 检查首页、产品切换、深浅登录、注册按钮可达性、前进后退和登录回跳，无页面脚本错误、缺失图片或横向溢出。另检查 320px 和 390px 英文首页与注册表单。没有创建真实账号或启动真实任务；未重跑完整后端测试。保留既有构建分块体积警告，自动浏览器为 Chromium，不冒充 Edge 实测。

截图位于 `/tmp/qingxuan-entry/`。首页、登录和构建入口均已返回 HTTP 200，开发服务保持运行。

## 已实现

- 青玄品牌、深浅主题、分组导航、运行切换和上下文详情。
- 真实快照的资产、发现、任务、角色最近活动与事件总览；显示数据覆盖范围。
- 发现和证据定位、流量深链接与筛选、重放草稿保护、历史连接状态。
- 独立只读大屏，采用已交付的 V2 背景、Three.js 资产拓扑、任务与发现侧区、事件焦点。
- 大屏支持图模式、平面回退、节点选择、标准/远距字号、全屏、暂停与单次刷新。
- 轮询暂停、隐藏页面、运行切换与登录失效时的请求及渲染清理。

## 验证范围

用户在实施中要求减少测试和审查，后半程采用构建、已有组件的定向检查和基础浏览器验收，不再执行逐项审查循环。

- 完整构建通过；保留依赖分块体积警告，Three.js 与大屏代码按需加载。
- 前半程前端完整测试最多验证至 165 项通过；后续功能调整使用定向检查，未重跑完整测试矩阵。
- 后端完整测试为 854 项：853 通过、1 项平台相关跳过、0 失败。
- 工作台在桌面和手机验证了主题、发现下钻、详情关闭及浏览器前进后退。
- 大屏在 1920、1366、390、3840 CSS 像素宽度检查了布局、图片和业务 Canvas；另检查 4K 远距模式。
- 大屏基础交互覆盖节点选择后的像素变化、暂停后的单次刷新、全屏及 WebGL 上下文丢失后的平面回退。
- 浏览器数据夹具仅位于验收脚本中，未作为生产回退。没有通过验收启动真实渗透任务。

## 当前边界

- 自由文本筛选在视图重新挂载时重置，安全的 URL 筛选和实体定位可恢复。
- 大屏新增控件目前优先中文，工作台原有语言切换保留。
- 未执行 30 分钟最大负载与完整设备兼容性测试；本次通过基础可用性检查，不宣称长期性能已达标。

实现进度见 [实施计划](superpowers/plans/2026-09-15-qingxuan-frontend-implementation.md)。本地截图位于 `/tmp/qingxuan-ui/`，4K 远距截图位于 `/tmp/qingxuan-ui-distance/`。

## 大屏视觉修订

- 修复二维图继承 210px 索引栏宽度的问题，证据和任务画布填满中央区域；单独提高大屏图谱标签字号。
- 移除统计、侧区、底部的黑色背景块和重复标题背景，保留用户生成的整页背景及透明分区。
- 中文标题采用本地 Noto Sans SC 展示字重，数字采用 Rajdhani；字体授权文件随 `web/public/fonts/` 分发。
- 新增任务状态环图、资产构成环图、事件折线，均来源于已加载数据。数据覆盖未知时不伪造零事件。
- 三维资产移除方形底板，增加设备轮廓和服务器灯条；保留真实拓扑关系与事件高亮。
- 重新检查桌面、手机及 4K 截图、图谱宽度、图表渲染和基础交互。修订截图见 `/tmp/qingxuan-wall-final/`；未追加完整测试及长期性能审查。

### Edge 截图反馈后的修订

- 六组指标改为标题、数字、覆盖说明纵向居中，增加组间间距；避免上一组数值靠近下一组标题。
- 大屏采用独立透明 Ant Design 容器主题；移除节点标签、二维节点和底部控制条的实色底。弹出的菜单仍有可读底色，不改变工作台主题。
- 使用得意黑原版展示字体和 Chakra Petch 仪表数字，环图增加细刻度外圈；所有字体及授权本地分发。
- 调整三维设备比例与材质，按实际占用范围取景，主机标签优先。只改变绘制方式，不生成新资产和连线。
- 使用更接近用户截图数量的浏览器专用夹具，检查全屏、1536 CSS 像素宽度下的布局和透明底色；未启动真实任务。浏览器自动检查使用 Chromium，Edge 的实际视觉仍以用户截图反馈为准。

新版截图位于 `/tmp/qingxuan-v3-final/`，全屏和紧凑排版截图位于 `/tmp/qingxuan-v3-layout/`。

### V3 平面背景接入

用户确认换背景路线，并提供新的 3840×2160 PNG。大屏现使用 `wallboard-surface-v3-1080.webp` / `wallboard-surface-v3-4k.webp`，提供 PNG 兼容回退；旧舱室背景保留但不再引用。没有新增遮罩、重绘源图或更改业务逻辑。保持现有透明图表分区、仪表字体与中央三维拓扑，避免背景与信息层的透视冲突。

本轮前端构建通过，1080p、4K 全屏和手机的图片加载、布局与 Canvas 检查通过。截图位于 `/tmp/qingxuan-flat-background/`，使用浏览器专用验收数据。
