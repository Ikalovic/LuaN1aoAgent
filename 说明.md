# FOFA MCP 修改说明

## Beekeeper MCP 追加说明

本次继续增加了 Beekeeper 凭据库 MCP 能力，目的是让 Executor 模型可以自主查询、写入和标记凭据状态。该能力不限制域名；`query_credentials` 可以指定 `domain`，也可以不指定从全库分页查询。

新增文件和用途：

- `scripts/beekeeper-mcp-adapter.py`：项目内最小 stdio MCP adapter，复用 Beekeeper 的 SQLAlchemy schema/ORM，暴露三个工具，不暴露数据库路径或任意 SQL。
- `src/beekeeper/beekeeper-config.ts`：读取 `BEEKEEPER_*` 环境变量，构造子进程白名单环境。
- `src/beekeeper/beekeeper-types.ts`：Beekeeper MCP 工具结果类型。
- `src/mcp/beekeeper-runtime.ts`：宿主侧 MCP runtime，负责启动 adapter、校验工具集合、超时、解析结果和写入无敏 metric。
- `src/tools/beekeeper-mcp-tools.ts`：把 Beekeeper MCP 暴露为 Executor 工具。
- `src/controller.ts`：在 Run 启动时初始化 Beekeeper MCP，向 Executor 注入工具，停止/关闭时释放 runtime。

新增工具：

| 工具 | 用途 |
| --- | --- |
| `query_credentials` | 明文查询凭据，支持 `domain`、`cursor`、`limit`、`include_invalid` |
| `store_credential` | 写入单条凭据；新记录默认 `is_valid=true` |
| `mark_credential_invalid` | 只有在明确认证失败时，将凭据标记为无效 |

查询是按次限制，不是全局限制。默认每次最多 50 条，可通过 `BEEKEEPER_MCP_MAX_PAGE_SIZE` 调整。返回 `has_more=true` 时会带 `next_cursor`，模型可以继续分段读取；但提示词和工具说明明确要求：如果测试到一个可用凭据，就停止继续拉取全量数据。

配置示例：

```env
BEEKEEPER_MCP_ENABLED=1
BEEKEEPER_ROOT=/home/ika/LuaN1aoAgent/LuaN1aoAgent/vendor/Beekeeper
BEEKEEPER_MCP_PYTHON=/home/ika/LuaN1aoAgent/LuaN1aoAgent/.beekeeper-mcp-venv/bin/python
# BEEKEEPER_DATABASE_URL=sqlite:////absolute/path/to/beekeeper.db
BEEKEEPER_MCP_MAX_PAGE_SIZE=50
BEEKEEPER_MCP_TIMEOUT_MS=15000
```

首次使用当前项目专用 Python 环境：

```bash
python3 -m venv .beekeeper-mcp-venv
.beekeeper-mcp-venv/bin/python -m pip install -e vendor/Beekeeper
```

Web 面板确认方式：启动任务后，在 Trace/Event 里搜索 `beekeeper_mcp_ready` 和 `beekeeper_mcp_call`。前者表示 MCP 已启动并注入 Executor；后者表示模型实际调用了 Beekeeper MCP。metric 不写入 account/password，但工具调用结果会明文进入模型上下文和会话日志，这是本次按你的选择保留的行为。

## 1. 修改目的

本次修改为 LuaN1aoAgent 增加基于 MCP（Model Context Protocol）的 FOFA 资产发现能力，使 Task Executor 能在明确授权的 Scope 内查询公网资产，同时避免凭据泄露、越权查询、旁站资产被自动纳入测试范围，以及并发或翻页绕过配额限制。

FOFA 在此处仅作为资产发现和情报来源。查询结果不能直接扩大用户授权的 Scope，也不能单独作为主动扫描或漏洞利用的授权依据。

## 2. 新增能力

动态 Task Executor 在宿主机配置有效的 `FOFA_API_KEY` 后会获得以下五个工具：

| 工具 | 用途 |
| --- | --- |
| `fofa_account_info` | 获取脱敏后的账户能力、会员状态和积分信息 |
| `fofa_search` | 使用经过 Scope 校验的 FOFA 查询语句搜索资产 |
| `fofa_search_next` | 使用 Runtime 生成的不透明游标继续查询下一页 |
| `fofa_stats` | 对已授权查询执行 FOFA 统计聚合 |
| `fofa_host_aggregate` | 查询已授权主机或可信派生资产的聚合信息 |

这些工具只注册到 Executor。Planner、Supervisor 和 Projector 不会获得 FOFA 工具，也无法直接读取 MCP 私有上下文。

## 3. 整体架构

调用链如下：

```text
Task Executor
    │ 仅能看到公开、受限的 Pi 工具参数
    ▼
FOFA 工具桥接层
    │ 注入 taskRef，保存完整 Artifact，生成有界摘要
    ▼
宿主侧 FOFA Runtime
    │ 注入可信 Scope、管理配额和不透明游标
    ▼
stdio MCP 子进程
    │ 再次校验 Scope，持有 FOFA 凭据
    ▼
FOFA HTTPS API
```

主要实现文件：

- `src/fofa/fofa-config.ts`：配置装载、子进程环境白名单和凭据脱敏。
- `src/fofa/fofa-client.ts`：FOFA HTTP 客户端、响应校验、超时、取消和有限重试。
- `src/fofa/fofa-query.ts`：FOFA 查询语法解析和布尔分支分析。
- `src/fofa/fofa-scope-policy.ts`：Scope 锚定校验和结果分类。
- `src/mcp/fofa-server.ts`：提供五个 stdio MCP 工具。
- `src/mcp/fofa-runtime.ts`：MCP 生命周期、配额、游标、错误归一化和日志控制。
- `src/tools/fofa-mcp-tools.ts`：将 MCP 能力桥接为 Executor 工具。
- `src/controller.ts`：在 Scope 确定后启动 FOFA，并在 Run 结束时关闭。
- `src/stores/runtime-store.ts`：持久化每个 Task 的 FOFA 配额。

## 4. Scope 与旁站约束

### 4.1 查询必须锚定授权资产

每个 FOFA 查询的每一个 `OR` 分支都必须保留至少一个正向授权锚点，例如：

- 已授权域名或其允许的子域；
- 已授权 IP 或 CIDR；
- Runtime 认可的可信派生引用。

包含不受支持的语法、无法确定身份的条件、缺少授权锚点的分支或过度复杂的表达式时，查询会默认拒绝。

### 4.2 子域和旁站的区别

- 授权根域按 DNS 标签边界包含其子域，符合条件的子域可分类为 `in_scope`。
- 共享 IP 上的其他域名、证书关联、ICP 关联和身份不明确的资产只能分类为 `candidate_only`。
- `candidate_only` 结果固定标记 `active_testing_allowed: false`。
- 候选结果不会写回或扩大原始 Scope，Executor 不能据此自动开展主动测试。

因此，严格约束不会阻止查询已授权目标的子域和旁站线索，但会阻止系统把旁站线索误当成已授权目标。

## 5. 凭据安全

- `FOFA_API_KEY` 和可选的 `FOFA_EMAIL` 只保存在宿主侧 MCP 子进程环境中。
- 凭据不会进入 Executor 沙箱、模型提示词、公开工具参数、Task Artifact 或普通运行事件。
- MCP 子进程只继承运行所需的环境变量白名单，不继承宿主机完整环境。
- Provider 错误、stderr 和关闭原因在写入 ExecutionLog 前会进行密钥、邮箱及 URL 参数脱敏。
- 未配置密钥时，Agent 仍可正常启动，只是不注册五个 FOFA 工具。
- FOFA 配置格式错误或初始化失败时，只禁用 FOFA，不影响其他 Executor 工具。

不得将真实密钥、卡密或 `.env` 文件提交到 Git 仓库。

## 6. 配额与并发控制

默认限制如下：

- 每次调用最多返回 100 条记录；
- 每个 Task 累计最多返回 1,000 条搜索记录；
- 每个 Task 最多执行 20 次统计或主机聚合调用；
- 单次请求默认超时 15,000 毫秒。

Runtime 在请求发出前通过 SQLite 事务原子预留配额，因此：

- 并发调用不能超额；
- 下一页查询不能绕过 Task 累计限制；
- `--resume` 后已消费配额仍然有效；
- 只有能够确认请求尚未发出的失败才释放预留配额；
- 无法确认 Provider 是否已经消费积分时，配额保持已消费状态。

Provider 的真实翻页令牌只保存在 Runtime 内存中。模型只能看到随机生成的 Task 绑定游标，游标不能跨 Task 使用，在 Task 终止、超时或进程重启后失效。

## 7. 结果与 Artifact

完整 FOFA 结果会保存为 Task JSON Artifact，媒体类型为：

```text
application/vnd.luanniao.fofa+json
```

模型只接收最多约 12 KB 的摘要，其中包含：

- 查询和字段信息；
- 返回数量和配额状态；
- `in_scope` 与 `candidate_only` 数量；
- 少量分类后的结果预览；
- 完整结果的 `artifactRef`；
- 候选资产禁止主动测试的明确提示。

这种设计既保留完整审计材料，又避免大量 FOFA 数据占满模型上下文。

## 8. 生命周期和容错

- Controller 只有在 Run 的授权 Scope 已确定后才启动 FOFA MCP。
- Run 停止、Controller 关闭或初始化失败时会关闭 MCP 连接和子进程。
- 子进程异常后，Runtime 最多为后续调用自动重启一次。
- 发生传输中断的调用不会自动重放，避免重复查询或重复消耗积分。
- AbortSignal 会传递到 MCP 调用和 FOFA HTTP 请求。
- Task 到达最终状态时会清除其所有内存游标。

## 9. 配置方式

在项目本地 `.env` 中配置，示例：

```env
FOFA_API_KEY=your-official-fofa-api-key
#FOFA_EMAIL=your-account@example.com
FOFA_API_BASE_URL=https://fofa.info
FOFA_MAX_RESULTS_PER_CALL=100
FOFA_MAX_RESULTS_PER_TASK=1000
FOFA_MAX_AGGREGATIONS_PER_TASK=20
FOFA_REQUEST_TIMEOUT_MS=15000
```

其中 `FOFA_API_BASE_URL` 默认使用 FOFA 官方 HTTPS 地址。生产环境拒绝明文 HTTP；只有 `NODE_ENV=test` 下的本机回环地址可以使用 HTTP，以便运行无外部请求的模拟测试。

当前实现面向 FOFA 官方 API。第三方中转站的接口路径、认证方式和响应格式可能不兼容，尤其不应通过明文 HTTP 发送密钥、查询目标或其他敏感信息。

## 10. 测试与验证

普通测试全部使用本地 mock，不访问 FOFA，也不会消耗积分。覆盖内容包括：

- 五个 API 操作及参数编码；
- 查询语法、Scope 锚定和旁站分类；
- 凭据与错误信息脱敏；
- 超时、取消、有限重试和错误归一化；
- 并发配额、分页配额和恢复后的配额持久化；
- Task 游标隔离、过期与失效；
- MCP 子进程启动、关闭、异常恢复和孤儿进程检查；
- Executor 工具隔离、有界摘要和完整 Artifact。

完整验证结果：

- Node：678 个通过，1 个平台相关用例跳过；
- Web：59 个通过；
- Python：50 个通过；
- `traffic-proxy`、`gateway-tun` 和 `socks-connector` 三个 Go 模块通过。

如需执行一次真实的一条记录冒烟测试，必须显式启用：

```bash
npm run build
FOFA_LIVE_TEST=1 FOFA_API_KEY=... npm run test:fofa-live
```

该命令会产生真实 FOFA 请求，可能消耗账户积分，不应在普通 CI 中启用。

## 11. 参考来源

端点映射和行为参考了 FOFA 官方 SDK，以及采用 MIT 许可证的 fofaEX 3.3.1。项目没有复制或捆绑 fofaEX 源码。

## CTF 与渗透测试双模式

Web 启动任务时可选择 `CTF 题目` 或 `渗透测试`，省略 `taskType` 时默认使用渗透测试。CTF 完成后生成 `writeup.md`；渗透测试完成后生成 `pentest-report.md`。

渗透测试使用 Markdown 模板，不依赖原始 PDF。默认模板位于：

- `templates/pentest/default-scoring-standard.md`
- `templates/pentest/default-report-template.md`

可通过环境变量替换：

```bash
PENTEST_SCORING_TEMPLATE=/path/to/scoring.md
PENTEST_REPORT_TEMPLATE=/path/to/report-template.md
```

报告模板只规定格式和评分字段，报告中的漏洞、攻击路径、证据和分数必须来自当前授权运行的真实事件或 Artifact。模板缺失时，渗透测试运行不会启动；CTF 模式不读取这些模板。

## FOFA 候选资产与作战图

FOFA 返回的子站、旁站、CNAME、证书关联主机、端口和服务会整理为 Operation Graph 候选节点，并保留 `artifact:*` 证据引用。候选节点使用 `classification=candidate_only`、`validationStatus=pending` 和 `active_testing_allowed=false`。

Planner 会为待验证候选创建一次验证任务；Executor 的 `validate_candidate_asset` 仅执行 DNS、HTTP、TLS、CNAME 和重定向等低风险检查。验证结果由 Observer 通过 `graph_delta_submit` 合并回作战图，补充 `resolves_to`、`has_alias`、`validated_by` 和验证信号。验证成功不会扩大授权 Scope，也不会自动开始漏洞扫描或利用。
